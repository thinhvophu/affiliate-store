# Implementation Plan — US00034: Affiliate URL helper & no-raw-anchor convention

**Status:** Done
**Feature:** F0003 — Affiliate Link Routing
**Spec ref:** `docs/specs/F0003.md` § US00034
**Predecessors / siblings:**

- **Sibling US00031** — Canonical `<AffiliateLink>` primitive (`components/AffiliateLink.tsx`). Owns link semantics + accessible label.
- **Sibling US00032** — Whole-card clickability pattern (no nested interactives). Documents the wrap-a-card recipe co-located with `<AffiliateLink>`.
- **Sibling US00033** — Tracking-hook seam ready for F0007. Owns the `data-affiliate-link` / `data-product-*` attribute contract on `<AffiliateLink>`.
- **Upstream dep — F0001** (`lib/products.ts`, `types/product.ts`). The helper from this story is wired into `lib/products.ts`'s `validateProduct()` so an unrecognized affiliate host fails the build at content-load time, not at runtime. The loader, the `Product.affiliateUrl` field, and the `[content] <path>: …` error-message style already exist and must be reused.
- **Downstream consumer — F0007** (Analytics & Click Tracking). Not touched here; this story only re-affirms the tracking-seam contract by repeating the no-raw-anchor convention in `CLAUDE.md`.

This story is the **content-validation half** of F0003. US00031–US00033 build the runtime primitive; US00034 builds the build-time guarantee that every `affiliateUrl` shipped through `lib/products.ts` is a known-good Shopee URL before any page consumes it, and documents the project-wide rule that nothing other than `<AffiliateLink>` may emit a Shopee-host anchor.

---

## 1. Scope

Strictly:

1. Create a new pure-utility module **`lib/affiliate.ts`** that exposes:
   - A `const SHOPEE_AFFILIATE_HOSTS` array containing exactly `["shopee.vn", "shopee.ee", "shope.ee"]`.
   - `assertAffiliateUrl(url: string, productSlug: string): string` — validates the input string is a parseable absolute URL and that its host is in the allow-list. Returns the input on success; throws a `[content] product "<slug>": …` error on failure. The single normalization/parsing chokepoint for all affiliate URLs in the project (spec Scenario 4).
   - `isAffiliateHost(host: string): boolean` — small internal predicate; exported so future call sites that need a non-throwing check can avoid re-implementing the allow-list.
2. Wire `assertAffiliateUrl()` into **`lib/products.ts`**'s `validateProduct()` so that any product JSON whose `affiliateUrl` is malformed, empty, or hosted off-list fails `npm run build` with a clear, single-line error message naming the offending product slug and the disallowed host (spec Scenarios 1–3).
3. Update **`CLAUDE.md`** "Codebase structure" → `lib/` listing + Module conventions, and **append a no-raw-anchor paragraph to the existing top-level "Affiliate links" section** (created by US00031 + US00033 per the Q4 design decision):
   - Add the new `lib/affiliate.ts` row to the codebase tree.
   - Bump the "Last updated" line.
   - Append to the "Affiliate links" section: _"All affiliate destinations on the site must route through `<AffiliateLink>` (`components/AffiliateLink.tsx`). Raw `<a href="https://shope.ee/…">` (or any Shopee-host anchor — `shopee.vn`, `shopee.ee`, `shope.ee`) outside `<AffiliateLink>` is disallowed. URL validation, parsing, and any future normalization live in `lib/affiliate.ts`; no other file may parse or rewrite affiliate URLs."_
4. Mirror the no-raw-anchor rule in the **`README.md`** "Codebase structure" section so that contributors who only read the public-facing README see it too.
5. Add the same convention statement as a top-of-file JSDoc block inside `lib/affiliate.ts` and (per US00031) as a comment in `components/AffiliateLink.tsx`'s usage example. (US00031 owns the component file; this story only writes the rule down — the actual example code lives in the US00031 plan.)
6. Add tests via a **build-time regression check**: a temporary scratch product JSON with a bad host is used to verify the loader throws the documented error, then is removed before commit. The mechanism is documented in §6 / §7 so the implementer (and any reviewer of `/implement-story`) can re-run it.

**Explicitly out of scope** (owned by sibling stories or downstream features):

- The `<AffiliateLink>` component itself, its accessible label, and the `data-affiliate-link` / `data-product-*` attribute set → **US00031 + US00033**.
- The whole-card clickability example and `AffiliateLink.module.css` (if any) → **US00032**.
- A custom ESLint rule that flags `<a href="https://shope.ee/…">` outside `<AffiliateLink>` → spec §2 explicitly defers this to P2. Convention doc + code review is the v1 enforcement.
- Stripping or normalizing tracking-bait query params on the affiliate URL (e.g., `utm_*`, `aff_sub`, etc.) → spec §5 notes the chokepoint exists in `lib/affiliate.ts` so a future single-file edit can add it. Not implemented now.
- Multi-network support (Lazada, Tiki, etc.) → spec §2 out of scope.
- Any change to `lib/posts.ts` — posts do not carry an `affiliateUrl` field. Affiliate URLs flow through embedded `<ProductCard slug="…" />`, which resolves to a product whose URL is already validated by this story's `lib/products.ts` integration.
- Any change to existing product JSON files. The five seeded fixtures already use `shope.ee` URLs (verified during planning) and will pass validation unchanged.

---

## 2. Pre-conditions / current state

- Working dir: `/Users/thinh/work/swe/aff-store/`. Branch at planning time: `main`. Clean apart from untracked screenshots + Playwright CLI dir (verified).
- `lib/products.ts` (F0001) already validates every required field on a product JSON and throws `[content] <filePath>: …` errors. The required-string list **already includes `affiliateUrl`** (`requiredStrings` array, line 13–20) which guarantees the field is a non-empty string before this story's helper sees it. The helper is therefore responsible only for **URL well-formedness and host allow-listing**, not the empty-string check.
- `lib/products.ts` exports `getAllProducts()` and `getProductBySlug()`. `getProductBySlug()` calls `getAllProducts()` internally, so adding validation inside `validateProduct()` covers both surfaces.
- `types/product.ts` defines `Product.affiliateUrl: string`. No change needed — the helper accepts a `string` and the typed field stays a `string`.
- `content/products/*.json` exists with seeded fixtures. Spot-check (planning): all current URLs use `shope.ee` host → unchanged by this story.
- `components/` does not yet contain `AffiliateLink.tsx`; US00031 will add it. This plan does not depend on US00031 having landed first — the helper and convention are independently shippable. (If US00031 ships first, this story simply has a real consumer to document; if US00034 ships first, the helper protects every future call site from day one.)
- `tsconfig.json` exposes the `@/*` path alias; the loader will import via `import { assertAffiliateUrl } from "@/lib/affiliate";`.
- No new npm dependency. `URL` is a Node.js global (also available in Edge / browser runtimes).
- Next.js docs reviewed (per AGENTS.md): no Next.js-specific API is used by this story. The validation runs inside the build-time data-loader path (`lib/products.ts`), which is already executed during `next build` when pages call `getAllProducts()` / `getProductBySlug()`. Reference docs cross-checked:
  - `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md` — confirms `lib/*` modules run on the server (during SSG/SSR), which is where the URL parse needs to happen. Helper has no `"use client"` directive.
  - `node_modules/next/dist/docs/01-app/03-api-reference/02-file-conventions/01-default.mdx` (route conventions) — confirms `lib/` is not a special Next.js folder; pure helper modules are free to live there. No conflict.

---

## 3. Approved Decisions

| #   | Decision                                  | Choice | Rationale |
| --- | ----------------------------------------- | ------ | --------- |
| D1  | Helper file location                      | **`lib/affiliate.ts`** — pure TypeScript, no React | Matches the structure-doc rule "Data loaders / formatters in `lib/` (e.g., `lib/products.ts`, `lib/posts.ts`). No JSX in `lib/`." Mirrors `lib/disclosures.ts` (US00022) as a precedent for project-wide content constants & validators. |
| D2  | API shape                                 | **`assertAffiliateUrl(url, productSlug): string`** (throws on failure, returns `url` on success). Plus **`isAffiliateHost(host): boolean`** as a small internal predicate, exported for future non-throwing call sites. Plus **`SHOPEE_AFFILIATE_HOSTS`** exported as a `readonly` tuple. | Assertion style matches the spec wording ("e.g., `assertAffiliateUrl(url, productSlug)`") and the existing throw-style of `lib/products.ts`. Returning `url` keeps it composable (`product.affiliateUrl = assertAffiliateUrl(obj.affiliateUrl, obj.slug)`). The boolean predicate exists so we never have to re-list `["shopee.vn", …]` anywhere else (spec Scenario 4: single chokepoint). The const export is a public part of the contract so reviewers can cite it. |
| D3  | URL parsing strategy                      | **`new URL(input)`** wrapped in try/catch. On `TypeError` (malformed / non-absolute), rethrow with the `[content] product "<slug>": affiliateUrl is not a valid absolute URL …` message. On success, take `parsed.hostname.toLowerCase()` and compare against the allow-list. | Spec §6 explicitly suggests `new URL(url)`. `URL` enforces absolute URLs (relative paths throw). `.hostname` strips port, userinfo, query, and is already lower-case for ASCII inputs — calling `.toLowerCase()` makes the IDN / mixed-case case explicit. Subdomain matching is **not** implied — `s.shopee.vn` would fail, which the spec wants (allow-list is exact-host). Verified by reading the project's product fixtures: all use the bare `shope.ee` host with no subdomain. |
| D4  | Allow-list contents                       | **Exactly `["shopee.vn", "shopee.ee", "shope.ee"]`** — no subdomains, no `www.` prefix, no other Shopee TLDs. Stored as a `readonly` tuple so types narrow nicely. | Verbatim from spec §2 and §6. If the user later adopts `affiliate.shopee.vn`-style deep links, only this constant changes — single-line edit. `www.` is unusual for Shopee affiliate URLs and is not in the seeded fixtures; treating it as "not allowed" surfaces a clean error rather than silently accepting an unintended host. |
| D5  | Error-message format                      | **`[content] product "<slug>": affiliateUrl host "<host>" is not an allowed Shopee host (allowed: shopee.vn, shopee.ee, shope.ee).`** for host failures; **`[content] product "<slug>": affiliateUrl "<value>" is not a valid absolute URL.`** for parse failures. Single line each. | Mirrors the existing `[content] ${filePath}: …` style in `lib/products.ts`. Using `product "<slug>"` (the slug, not the file path) is what the spec asks for — "naming the offending product slug" (Scenarios 2 and 3). The slug is more useful than the path for downstream readers because it's the URL key, but for searchability the loader still also throws via its existing wrapping that includes the file path; see D7 for the layering. |
| D6  | Where the convention lives in CLAUDE.md   | **Append a no-raw-anchor paragraph to the top-level "Affiliate links" section** created by US00031 + US00033 (between "UI / theme rules" and "Required disclosures"). Also add a short pointer in the "Codebase structure" → "Module conventions" sub-list cross-referencing `lib/affiliate.ts` as the validation chokepoint. | Per the Q4 design decision: F0003 conventions belong in one discoverable section, not scattered across "Working conventions" / "UI / theme rules". US00031 + US00033 already create the section; US00034 simply appends a sibling paragraph. The "UI / theme rules" affiliate-link line stays where it is (it documents the `target`/`rel` requirement on the primitive itself), but the broader convention now lives in the dedicated section. |
| D7  | How the helper is wired into `validateProduct()` | **Call `assertAffiliateUrl(obj.affiliateUrl as string, obj.slug as string)` from inside `validateProduct()` *after* the existing `requiredStrings` loop has already confirmed both fields are non-empty strings.** Wrap the call so any thrown error gets re-thrown with the file path prepended to the message: `throw new Error(\`[content] \${filePath}: \${err.message}\`);` — preserves the existing "include file path" debugging affordance while also surfacing the slug. | The `requiredStrings` loop already guarantees the cast is safe. Re-throwing with the file path keeps tooling like editor "click to open" working for the implementer; the slug-bearing inner message is what the spec demands for clarity. This is the cheapest, narrowest edit to `lib/products.ts`. |
| D8  | Tests / verification                      | **Build-time regression test** described in §6 step 7: temporarily drop a bad-host JSON into `content/products/`, run `npm run build`, capture the error, delete the file. No new test framework. The project has no unit-test harness today (none of US00011–US00026 added one) — adding Jest/Vitest is out of scope for this story. | Adding a test runner for one helper is overkill. The build itself **is** the regression test: if a future product JSON ever uses a non-Shopee host, the build fails. That is the acceptance criterion. The harness in §6 lets the implementer prove the failure path works once, before committing. |
| D9  | Branch strategy                           | **Feature branch `feat/US00034`, PR to `main`.** F0003 stories ship as four independent PRs in dep order (US00031 → US00032 → US00033 → US00034, or US00034 first since it has no runtime sibling deps). | Per CLAUDE.md "Working conventions". US00034 can land first because it has no dependency on US00031–US00033 (the helper and convention doc compile and run independently). |
| D10 | TypeScript strictness                     | **All exports fully typed.** No `any`. Use `readonly string[]` for the host list and `string` for the URL/slug params. The `assertAffiliateUrl` return type is `string` (the validated input) — this lets call sites use the return value as if it were a branded string in future, without a breaking-change rewrite. | Spec §6 emphasizes the F0003 ↔ F0007 contract stability; precise types make the helper's role obvious and prevent silent mis-use. |
| D11 | What about empty-string / whitespace      | **The existing `requiredStrings` check in `lib/products.ts` already trims+rejects empty `affiliateUrl`.** This helper does **not** re-do that — it assumes a non-empty string. If the helper is later called from a different path (e.g., MDX frontmatter for an inline product mention), that caller is responsible for non-empty validation, and the `new URL("")` call will throw `TypeError: Invalid URL` which our catch wraps with the helpful message anyway. Defense in depth. | Avoid double-validation in `validateProduct()`. The catch in `assertAffiliateUrl` is the safety net for any future caller that skips the upstream check. |

---

## 4. Files to create / modify

### 4.1 Create

| Path               | Purpose |
| ------------------ | ------- |
| `lib/affiliate.ts` | Single source of truth for affiliate-URL validation. Exports `SHOPEE_AFFILIATE_HOSTS`, `isAffiliateHost(host)`, `assertAffiliateUrl(url, productSlug)`. Top-of-file JSDoc states the no-raw-anchor convention so anyone editing the file sees the rule. ~50 lines including JSDoc. |

### 4.2 Modify

| Path             | Change |
| ---------------- | ------ |
| `lib/products.ts` | Inside `validateProduct()`, after the `requiredStrings` loop validates `affiliateUrl` is a non-empty string, call `assertAffiliateUrl(obj.affiliateUrl as string, obj.slug as string)` inside a try/catch that re-throws with the file path prepended. ~6 lines added. No other change. |
| `CLAUDE.md`     | (1) "Codebase structure" → `lib/` listing: add the `affiliate.ts` row. (2) Bump the "Last updated" line to reference US00034 (extending whatever wave the line currently shows — e.g., `US00031 + US00032 + US00033 + US00034`). (3) "Module conventions" sub-list: add a bullet pointing at `lib/affiliate.ts` as the single chokepoint. (4) **Append a no-raw-anchor paragraph to the existing top-level "Affiliate links" section** (US00031 + US00033 created the section; this story slots into the placeholder comment they left). Full text in §5.3. |
| `README.md`     | Mirror the structure-doc updates: the `lib/affiliate.ts` row in the `lib/` block and a short no-raw-anchor line (same wording as the new CLAUDE.md paragraph, abridged). |

### 4.3 Explicitly NOT created in this story

- `components/AffiliateLink.tsx`, `components/AffiliateLink.module.css`, and the component's usage-example docs → **US00031 / US00032 / US00033**.
- Any test-runner config (`jest.config.*`, `vitest.config.*`, `__tests__/`) — the build itself is the regression test (D8).
- Any new path alias, route, or `app/` change — this story is entirely under `lib/` + docs.

---

## 5. Concrete diff sketch

### 5.1 `lib/affiliate.ts` (new)

```ts
/**
 * Affiliate URL validation — F0003 / US00034.
 *
 * Single chokepoint for parsing, validating, and (in the future) normalizing
 * Shopee affiliate URLs. No file outside this module may parse, trim, or
 * rewrite an affiliate URL.
 *
 * Project rule (mirrored in CLAUDE.md "UI / theme rules"):
 *   All affiliate destinations on the site MUST route through <AffiliateLink>
 *   (components/AffiliateLink.tsx). Raw <a href="https://shope.ee/..."> (or any
 *   Shopee-host anchor) outside <AffiliateLink> is disallowed. URL validation
 *   and any future normalization live HERE; no other file may parse or rewrite
 *   affiliate URLs.
 *
 * Allow-list is intentionally exact-host (no subdomain wildcards). A new
 * Shopee surface (e.g. `affiliate.shopee.vn`) is a single-line edit below.
 */

export const SHOPEE_AFFILIATE_HOSTS = ["shopee.vn", "shopee.ee", "shope.ee"] as const;

export type ShopeeAffiliateHost = (typeof SHOPEE_AFFILIATE_HOSTS)[number];

/** Non-throwing check; returns true iff `host` is in the Shopee allow-list. */
export function isAffiliateHost(host: string): host is ShopeeAffiliateHost {
  return (SHOPEE_AFFILIATE_HOSTS as readonly string[]).includes(host.toLowerCase());
}

/**
 * Validate that `url` is a parseable absolute URL whose host is in the Shopee
 * allow-list. Throws `Error` on failure with a single-line message naming
 * `productSlug` and (where known) the offending host.
 *
 * Returns `url` unchanged on success, so callers can write:
 *   product.affiliateUrl = assertAffiliateUrl(obj.affiliateUrl, obj.slug);
 */
export function assertAffiliateUrl(url: string, productSlug: string): string {
  let parsed: URL;
  try {
    parsed = new URL(url);
  } catch {
    throw new Error(
      `product "${productSlug}": affiliateUrl ${JSON.stringify(url)} is not a valid absolute URL.`,
    );
  }

  const host = parsed.hostname.toLowerCase();
  if (!isAffiliateHost(host)) {
    throw new Error(
      `product "${productSlug}": affiliateUrl host "${host}" is not an allowed Shopee host ` +
        `(allowed: ${SHOPEE_AFFILIATE_HOSTS.join(", ")}).`,
    );
  }

  return url;
}
```

### 5.2 `lib/products.ts` — diff fragment

Insert inside `validateProduct()`, after the `requiredStrings` loop completes (which currently runs at lines 13–27 in the file shown in §2):

```ts
// Insert after the requiredStrings loop and BEFORE the price/images/specs blocks.
//
// At this point obj.affiliateUrl and obj.slug are guaranteed non-empty strings
// by the requiredStrings loop above.
try {
  assertAffiliateUrl(obj.affiliateUrl as string, obj.slug as string);
} catch (err) {
  const inner = err instanceof Error ? err.message : String(err);
  throw new Error(`[content] ${filePath}: ${inner}`);
}
```

Plus the import at the top of the file:

```ts
import { assertAffiliateUrl } from "@/lib/affiliate";
```

### 5.3 `CLAUDE.md` — structural updates

- **`lib/` listing** — add row (keep alphabetical / logical order):
  ```
  lib/                 # Pure utilities, data loaders, formatters (no React)
  ├── affiliate.ts     # Shopee affiliate-URL allow-list + assertAffiliateUrl helper (US00034)
  ├── disclosures.ts
  ├── env.ts
  ├── nav-items.ts
  ├── posts.ts
  └── products.ts
  ```
- **"Last updated" line** at the top of "Codebase structure" — extend the wave:
  ```
  > Last updated: US00031 + US00032 + US00033 + US00034 (lib/affiliate.ts — Shopee affiliate-URL allow-list helper; wired into lib/products.ts so bad affiliateUrls fail the build)
  ```
- **"Module conventions" sub-list** — append:
  ```
  - **Affiliate URLs** are validated in one place: `lib/affiliate.ts`. The product loader (`lib/products.ts`) calls `assertAffiliateUrl(url, slug)` at build time, so any product JSON with a non-Shopee or malformed `affiliateUrl` fails `next build` with the offending slug named. No file outside `lib/affiliate.ts` may parse, trim, or rewrite an affiliate URL.
  ```
- **Append to the existing "Affiliate links" section** (created by US00031 + US00033, located between "UI / theme rules" and "Required disclosures"). The section already contains the F0003 ↔ F0007 contract paragraph and the US00032 whole-card pattern paragraph, plus a placeholder HTML comment `<!-- US00034 will append a no-raw-anchor bullet here on landing. -->`. Replace that placeholder with:

  ```markdown
  **No raw affiliate anchors (US00034).** All affiliate destinations on the site must route through `<AffiliateLink>` (`components/AffiliateLink.tsx`). Raw `<a href="https://shope.ee/…">` — or any anchor whose host is `shopee.vn`, `shopee.ee`, or `shope.ee` — outside `<AffiliateLink>` is **disallowed**; block such PRs on review. URL validation, parsing, and any future normalization live in `lib/affiliate.ts`; no file outside that module may parse, trim, or rewrite an affiliate URL. Allow-listed hosts: `shopee.vn`, `shopee.ee`, `shope.ee` (exact host match; subdomains and `www.` are intentionally excluded so unexpected hosts surface loudly).
  ```

  If the placeholder comment is missing (e.g., US00031 landed without it), append the paragraph at the end of the "Affiliate links" section.

### 5.4 `README.md` — same edits

Mirror the `lib/` row + the no-raw-anchor rule bullet in the README's Codebase structure block, so contributors who only read the public README also see the rule.

### 5.5 Diff summary

- 1 new file (`lib/affiliate.ts`, ~50 lines).
- 1 source file modified (`lib/products.ts`, +1 import, +6 lines inside `validateProduct()`).
- 2 doc files modified (`CLAUDE.md`, `README.md` — additive only).
- 0 changes to product JSON, types, components, routes, env vars, `next.config.ts`, `tsconfig.json`, `package.json`.
- 0 new npm dependencies.

---

## 6. Step-by-step execution order

(For the implementer who picks up the approved plan via `/implement-story US00034`.)

1. `git fetch origin && git checkout -b feat/US00034 origin/main`.
2. **Re-read** `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md` — confirm that modules under `lib/` are server-side by default and run during the SSG build. Per AGENTS.md the local docs are source of truth.
3. Create `lib/affiliate.ts` per §5.1. Include the top-of-file JSDoc verbatim — it is the in-code mirror of the convention.
4. Edit `lib/products.ts`:
   - Add `import { assertAffiliateUrl } from "@/lib/affiliate";` near the top.
   - Insert the try/catch from §5.2 inside `validateProduct()`, immediately after the `requiredStrings` loop.
5. Run `npm run typecheck` — must exit 0. (Catches signature drift between the helper and the loader.)
6. Run `npm run lint` — must exit 0.
7. **Build-time regression harness** (verifies the failure path; this is the test for D8):
   - `cp content/products/$(ls content/products | head -1) content/products/__bad-host__.json` to seed a temp product file.
   - Edit `content/products/__bad-host__.json`: change `slug` to `"__bad-host-test__"`, change `affiliateUrl` to `"https://example.com/whatever"`.
   - Run `npm run build`. Confirm it **fails** with a message containing both `product "__bad-host-test__"` and `not an allowed Shopee host`.
   - Replace `affiliateUrl` value with `"not-a-url-at-all"`. Run `npm run build` again. Confirm it fails with `product "__bad-host-test__": affiliateUrl "not-a-url-at-all" is not a valid absolute URL.`.
   - `rm content/products/__bad-host__.json`. Confirm `npm run build` now passes again.
   - Confirm `git status` shows only `lib/affiliate.ts`, `lib/products.ts`, `CLAUDE.md`, `README.md` modified — the harness must not leak into the commit.
8. Run `npm run build` one final time — must exit 0 with all real product fixtures intact.
9. Update `CLAUDE.md` per §5.3: last-updated line bump, `lib/affiliate.ts` row in the `lib/` tree, module-conventions bullet, and the no-raw-anchor paragraph appended to the existing "Affiliate links" section (replacing the placeholder comment US00031 left).
10. Update `README.md` per §5.4.
11. Run AC verification (Section 7).
12. Commit on the feature branch:
    `feat(US00034): add lib/affiliate.ts and wire build-time Shopee host allow-list into lib/products.ts`
13. Push and open PR. PR review = approval gate (per CLAUDE.md "Working conventions"). Plan Status flips Draft → Approved on plan approval; flips Approved → Done after merge.

---

## 7. Acceptance-criteria verification

| AC                                                  | Test                                                                                                                                                       | Pass condition                                                                                                                                                                                                       |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Scenario 1 — Helper validates Shopee hosts          | In a Node REPL: `node -e 'import("./lib/affiliate.ts").then(m => console.log(m.assertAffiliateUrl("https://shope.ee/abc", "x")))'`. Repeat for `shopee.vn` and `shopee.ee`.                                                                                                              | Each call returns the input string unchanged; no throw.                                                                                                                                                              |
| Scenario 2 — Unrecognized host fails the build      | §6 step 7 harness (bad host).                                                                                                                                                                                                                                                            | `npm run build` exits non-zero. Error message contains both the offending product slug and the disallowed host string.                                                                                               |
| Scenario 3 — Malformed URL fails the build          | §6 step 7 harness (garbage URL).                                                                                                                                                                                                                                                         | `npm run build` exits non-zero. Error message contains the offending product slug and the literal "not a valid absolute URL" phrase.                                                                                 |
| Scenario 4 — Helper is the single normalization point | `grep -RIn --include='*.ts' --include='*.tsx' -E '"(https?://)?(shopee\.vn\|shopee\.ee\|shope\.ee)"' .` excluding `lib/affiliate.ts`, the test product JSON files, and the helper's own JSDoc.                                                                                          | Zero hits in source code outside `lib/affiliate.ts` and outside the data files in `content/products/`.                                                                                                               |
| Scenario 5 — Convention is documented               | Read CLAUDE.md and README.md after edits.                                                                                                                                                                                                                                                | Both files contain a clearly worded sentence saying raw Shopee-host anchors outside `<AffiliateLink>` are disallowed, and naming `lib/affiliate.ts` as the validation chokepoint.                                    |
| Scenario 6 — Convention is enforceable in review    | Mentally walk through a hypothetical PR adding `<a href="https://shope.ee/xyz" target="_blank">…</a>` to a component.                                                                                                                                                                    | A reviewer can cite the new CLAUDE.md "UI / theme rules" bullet by name to block the PR — no tooling required.                                                                                                       |
| Typecheck / lint / build                            | `npm run typecheck && npm run lint && npm run build` (with the harness file removed).                                                                                                                                                                                                    | All exit 0. No new warnings.                                                                                                                                                                                          |
| Existing fixtures unaffected                        | Run `npm run build` with the real `content/products/*.json` files.                                                                                                                                                                                                                       | Build succeeds, all existing products load, no fixture had to be edited.                                                                                                                                              |

### Browser verification (per CLAUDE.md rule)

This story has no UI surface. Browser verification is **not applicable** — the acceptance criteria are entirely build-time. The implementer should still run `npm run dev`, open `http://localhost:3000`, and confirm the homepage renders normally (smoke test that `validateProduct()` did not regress for the seeded fixtures).

---

## 8. Codebase touchpoints

Quoting the entries from the structure doc (`CLAUDE.md` → "Codebase structure") that this story touches:

| Structure-doc entry                                                                                                                                  | How US00034 touches it                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lib/                 # Pure utilities, data loaders, formatters (no React)`                                                                          | Adds `lib/affiliate.ts` — a pure validation helper, no React, no JSX. Conforms to the rule.                                                                                                                                                                                                                          |
| `├── products.ts         # getAllProducts(), getProductBySlug() — reads content/products/*.json`                                                     | Modified: `validateProduct()` now calls `assertAffiliateUrl()` so bad URLs fail the build. Public surface (`getAllProducts`, `getProductBySlug`) is unchanged.                                                                                                                                                       |
| Module convention: "Data loaders / formatters in `lib/` (e.g., `lib/products.ts`, `lib/posts.ts`, `lib/format.ts`). No JSX in `lib/`."               | Honored. New file is pure TypeScript, no JSX.                                                                                                                                                                                                                                                                        |
| Module convention: "Imports use the `@/*` alias (e.g., `import { getProducts } from \"@/lib/products\"`). Avoid deep relative paths."                | Honored. `lib/products.ts` imports the new helper via `@/lib/affiliate`.                                                                                                                                                                                                                                             |
| `types/product.ts       # Product interface (canonical JSON shape)`                                                                                  | **Not modified.** `Product.affiliateUrl: string` is still the right type — the helper validates at runtime/build-time without narrowing the type.                                                                                                                                                                    |
| UI / theme rules → "Affiliate links: ALWAYS `target="_blank" rel="noopener noreferrer sponsored"`"                                                   | Unchanged here. The broader no-raw-anchor convention is appended to the top-level "Affiliate links" section (created by US00031 + US00033) rather than to "UI / theme rules" — per the Q4 design decision that consolidates affiliate conventions in one discoverable place.                                          |
| **New top-level "Affiliate links" section** (between "UI / theme rules" and "Required disclosures", created by US00031 + US00033)                  | Extended: appends a "No raw affiliate anchors (US00034)" paragraph that names `<AffiliateLink>` as the only sanctioned consumer of Shopee-host anchors and `lib/affiliate.ts` as the validation chokepoint. Replaces the placeholder comment US00031 left at the section's tail.                                       |
| Required disclosures block                                                                                                                           | **Not modified.** Independent of affiliate-URL validation.                                                                                                                                                                                                                                                           |
| `content/products/*.json` — Product JSON shape                                                                                                       | **Not modified.** Spot-check during planning confirmed all current `affiliateUrl` values use the `shope.ee` host and will pass validation unchanged.                                                                                                                                                                 |
| Working conventions: "All user-facing copy is Vietnamese. Code, identifiers, and comments stay in English."                                          | Honored. The error messages from `assertAffiliateUrl` are English (they are developer-facing build errors, never shown to visitors).                                                                                                                                                                                 |
| Out of scope (v1) → "Search (deferred to v2)" / "No accounts / no cart"                                                                              | Untouched.                                                                                                                                                                                                                                                                                                            |
| Env vars table                                                                                                                                       | Untouched.                                                                                                                                                                                                                                                                                                            |

Downstream consumers (not modified in this story):

- **US00031** (`components/AffiliateLink.tsx`) is the **only** sanctioned consumer of affiliate hrefs once it lands. The convention bullet added by this story names it explicitly. If US00031 lands later, the bullet still references the canonical file path — that file simply doesn't exist yet, which is acceptable (the rule applies prospectively).
- **F0004** (Product Catalog) and **F0006** (Editorial blog `<ProductCard slug="…" />`) will both import `<AffiliateLink>` once US00031 lands and will read `product.affiliateUrl` from `getProductBySlug()` — which by then is guaranteed validated.
- **F0007** (Analytics & Click Tracking) is unaffected — its contract is the `data-affiliate-link` attribute set defined by US00033, not the URL helper from this story.

---

## 9. Structure-doc impact

After implementation, update the **"Codebase structure"** section in both `CLAUDE.md` and `README.md`:

- **`lib/` listing:** add `affiliate.ts`.
  ```
  lib/
  ├── affiliate.ts     # Shopee affiliate-URL allow-list + assertAffiliateUrl helper (US00034)
  ├── disclosures.ts   # AFFILIATE_DISCLOSURE_VI constant (US00022)
  ├── env.ts
  ├── nav-items.ts     # NAV_ITEMS constant
  ├── posts.ts         # getAllPosts(), getPostBySlug()
  └── products.ts      # getAllProducts(), getProductBySlug() — now calls assertAffiliateUrl() at build time
  ```
- **"Last updated" line:** extend the wave to `US00031 + US00032 + US00033 + US00034 (lib/affiliate.ts — Shopee affiliate-URL allow-list helper; wired into lib/products.ts so bad affiliateUrls fail the build)`.
- **New "Module conventions" bullet:** affiliate URLs are validated in one place (`lib/affiliate.ts`), the loader calls the helper at build time, no other file may parse/rewrite affiliate URLs.
- **Append "No raw affiliate anchors (US00034)" paragraph** to the existing top-level "Affiliate links" section (created by US00031 + US00033) — replaces the placeholder comment that section ships with.

No new top-level folder. No new route. No new path alias. No `app/layout.tsx` change. No `tsconfig.json` change.

---

## 10. Risks / open items

- **Allow-list breadth.** Three exact hosts (`shopee.vn`, `shopee.ee`, `shope.ee`) — no subdomains, no `www.` prefix. If Shopee Vietnam's affiliate program ever issues URLs from `affiliate.shopee.vn` or `s.shopee.vn`, the build will fail until the constant is updated. Mitigation: single-line edit in `lib/affiliate.ts`; the spec wants exactly this strict behavior so unexpected hosts surface loudly rather than silently shipping.
- **Case sensitivity & IDN.** `.hostname` is already lowercase for ASCII; calling `.toLowerCase()` is defensive for IDN hosts. We do not handle `xn--` punycode equivalents (none expected for Shopee). If a URL like `https://SHOPEE.VN/abc` ever appeared, it would now pass — intended.
- **US00031 (`<AffiliateLink>`) may land after US00034.** The convention bullet references `components/AffiliateLink.tsx` by name; if the file doesn't exist yet when US00034 ships, the rule still applies prospectively. Reviewers can read the rule and block raw anchors even though the primitive isn't built yet — acceptable.
- **`<a href>` outside `<AffiliateLink>` is hand-enforced.** Custom ESLint rule is explicitly P2 (spec §2). The grep in §7 Scenario 4 lets reviewers spot violations cheaply; long-term, the lint rule becomes a follow-up story.
- **Helper not unit-tested in CI.** D8 documented why (no test runner in repo). The build-time harness in §6 step 7 covers the failure paths once, before commit. If the project later adds Vitest, a 10-line spec for this helper is the easiest first test to add.
- **Error message surface is developer-facing.** Wording is English on purpose — these messages only appear in `next build` output and never in the browser UI. Vietnamese is reserved for visitor-facing copy per CLAUDE.md working conventions.
- **`new URL()` accepts `mailto:` / `javascript:` / `data:` URLs as "valid".** Parsed `.hostname` for those is empty, which fails the allow-list check with "host '' is not an allowed Shopee host". Acceptable — produces a clear error rather than silently passing.
- **No runtime check at the primitive.** US00031's `<AffiliateLink>` does **not** re-validate the URL — it trusts that `assertAffiliateUrl` ran at build time. Acceptable because every product URL flows through `lib/products.ts`. If a future direct caller bypasses the loader (e.g., a hard-coded `<AffiliateLink href="https://example.com/" …>` in JSX), the convention bullet — not the runtime — is what catches it. This is the documented v1 enforcement model.

---

## 11. Definition of Done

- [x] `lib/affiliate.ts` created. Exports `SHOPEE_AFFILIATE_HOSTS` (readonly tuple), `isAffiliateHost(host)`, and `assertAffiliateUrl(url, productSlug)`. Top-of-file JSDoc states the no-raw-anchor convention.
- [x] `lib/products.ts` imports `assertAffiliateUrl` and calls it inside `validateProduct()`, with a try/catch that re-throws with `[content] <filePath>: …` prepended.
- [x] Existing seeded product JSON files in `content/products/` continue to load — `npm run build` exits 0 unchanged.
- [x] A product JSON with a non-Shopee `affiliateUrl` causes `npm run build` to fail with an error naming the offending product slug and the disallowed host (verified via the §6 step 7 harness).
- [x] A product JSON with a malformed `affiliateUrl` causes `npm run build` to fail with an error naming the slug and the literal "not a valid absolute URL" phrase (verified via the harness).
- [x] No source file outside `lib/affiliate.ts` parses, trims, or rewrites an affiliate URL (verified by the §7 Scenario 4 grep).
- [x] `CLAUDE.md` "Codebase structure" updated: `lib/affiliate.ts` row added, "Last updated" line extended to include US00034, "Module conventions" sub-list has the new affiliate-URL bullet. The top-level "Affiliate links" section (created by US00031 + US00033) has a new "No raw affiliate anchors (US00034)" paragraph appended (replacing the placeholder comment).
- [x] `README.md` mirrors the structure-doc updates.
- [x] `npm run typecheck`, `npm run lint`, `npm run build` all exit 0 with no new warnings.
- [x] The harness product JSON created during verification has been removed before commit; `git status` shows only the four intended files modified/created.
- [x] PR opened from `feat/US00034` to `main`; CI green; merged.
- [x] Plan Status flipped from `Draft` → `Approved` (on user approval of plan) → `Done` (after merge).
